<?php

use Illuminate\Support\Facades\Route;

/**
 * Authentication routes are now handled via JWT API endpoints.
 * 
 * Login:    POST /api/auth/login
 * Register: POST /api/auth/register
 * Logout:   POST /api/auth/logout (requires auth)
 * Me:       GET  /api/auth/me (requires auth)
 * 
 * For Inertia pages, create frontend pages that call these API endpoints.
 */

// Placeholder - all auth is now handled via API routes
// If you need web-based auth pages in the future, add them here
